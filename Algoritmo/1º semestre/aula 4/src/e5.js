// declarando a função soma que recebe 2 parâmetro
function somar(a, b){
    r = a + b;
    console.log(`${a}+${b}=${r}`);
}

// chamando a função somar passando 2 e 3 como parâmetro
somar(2,3);
// chamando a função somar passando 8 e 2 como parâmetro
somar(8,2);